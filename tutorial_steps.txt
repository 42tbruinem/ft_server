									symlink to goinfre to get docker to start.
rm -rf ~/Library/Containers/com.docker.docker
ln -s ~/goinfre/docker ~/Library/Containers/com.docker.docker
									we're using debianbuster, so we import that base image.
FROM debian:buster-slim
									install all the packages we need
RUN apt-get -y update && apt-get -y install mariadb-server \ wget \ php \ php-cli \ php-cgi \ php-mbstring \ php-fpm \ php-mysql \ nginx \ libnss3-tools
									copy the configuration file we set up, so the container can use it
COPY src ./root/
									set the directory to start the container in
WORKDIR ./root/
									run our configuration file
RUN $(WORKDIR)/


OS: debian buster
NGINX

Wordpress
phpMyAdmin

^ these work together with MySQL
SSL Protocol
redirect to the correct website
two options for nginx server, 1.autoindex on, 2.autoindex off
 
									